on:
  push:
    branches:
      - "main"
  pull_request:

name: "CI"

permissions:
  contents: read
  actions: read
  security-events: write

jobs:
  ci:
    uses: "WebProject-xyz/.github/.github/workflows/ci.yml@4796191a6df78f3002b078503545aeaba3977142" # main
    secrets: inherit
    with:
      php-version: "8.5"
      php-cs-fixer-ignore-env: true
      test-matrix: |
        [
          {"php-version": "8.5", "dependencies": "locked", "with_coverage": true}
        ]
